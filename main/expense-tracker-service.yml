apiVersion: v1
kind: Service
metadata:
  name: expense-tracker
  namespace: services
  labels:
    app: expense-tracker
spec:
  type: ClusterIP
  selector:
    app: expense-tracker
  ports:
    - name: http
      port: 80
      targetPort: 3000