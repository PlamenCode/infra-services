apiVersion: v1
kind: Service
metadata:
  name: hesk
  namespace: services

spec:
  type: ClusterIP

  selector:
    app: hesk

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP