apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hesk-db-data
  namespace: services

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: local-path

  resources:
    requests:
      storage: 2Gi