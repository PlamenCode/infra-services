apiVersion: v1
kind: Service
metadata:
  name: hesk-db
  namespace: services

spec:
  type: ClusterIP

  selector:
    app: hesk-db

  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP